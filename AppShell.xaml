<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HouseMovingAssistant.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:HouseMovingAssistant.Views"
    Shell.FlyoutBehavior="Disabled">

   <ShellContent Route="Login"
                  ContentTemplate="{DataTemplate local:LoginPage}">
    </ShellContent>

    <TabBar Route="Main">
        <ShellContent Title="Moving Tasks" Icon="furniture.png" Route="MovingTasks" ContentTemplate="{DataTemplate local:MovingTasksPage}" />

    </TabBar>

</Shell>
